# ==============================================================================
# CHESS AI CONFIGURATION - OPTIMIZED FOR RTX 5060Ti 16GB v4.2
# 🆕 POV (Point of View) + Dynamic Sliding Window Support
# ==============================================================================

# ------------------------------------------------------------------------------
# FILE PATHS (relative to chess/ project root)
# ------------------------------------------------------------------------------
paths:
  data_dir: "data"
  models_dir: "models"
  logs_dir: "logs"
  
  # Best models (single file, overwritten when improved)
  best_model_il: "models/best_model_il.pt"
  best_model_rl: "models/best_model_rl.pt"
  
  # Checkpoint directories (saved every N epochs/iterations)
  il_checkpoints_dir: "models/IL"
  rl_checkpoints_dir: "models/RL"


# ------------------------------------------------------------------------------
# DATA PROCESSING - 🆕 v4.3 TEMPORAL DISCOUNTING + v4.2 POV + SLIDING WINDOW
# ------------------------------------------------------------------------------
data:
  min_elo: 2300              # Minimum Elo rating for games
  max_games: 200000          # Top N games to use per PGN file
  train_split: 0.85           # Train/validation split ratio
  max_moves_per_game: 200    # Skip games longer than this
  
  # Quality filtering
  sort_by_avg_elo: true      # Sort by average Elo of both players

  # Game-level quality filters (headers + length)
  game_filters:
    enabled: true
    min_fullmove: 10              # Minimum fullmoves for any decisive game
    min_fullmove_draw: 20         # Stronger filter for short draws
    min_fullmove_resign: 12       # Early resignations
    max_elo_gap: 300              # Detect "rozkladówki"
    exclude_terminations:
      - "Time forfeit"
      - "Timeout"
      - "Flag"
      - "Abandoned"
      - "Rules infraction"
  
  # SLIDING WINDOW: dynamic history, no redundant storage
  sliding_window_stride: 2   # Sample every Nth position (1=all, 2=half, 3=third)
  
  # Position sampling by game progress (reduce opening duplicates)
  # keep_prob = keep_min + (keep_max - keep_min) * (progress ** power)
  # progress = move_idx / (total_moves - 1)
  position_sampling:
    enabled: true
    seed: 42
    min_fullmove: 3      # hard-skip first N fullmoves
    keep_min: 0.12       # early-game keep probability
    keep_max: 1.0        # late-game keep probability
    power: 1.8           # >1 biases toward mid/late game

  # Position deduplication (across games)
  # 🆕 v4.7: SMART DEDUP with max_count + position_plus_move mode
  position_dedup:
    enabled: true
    mode: "position_plus_move"   # position_plus_move | fen_no_counters | fen | pieces
    include_turn: true           # Include side-to-move in signature
    max_count: 3                 # Max occurrences per signature (None = remove all duplicates)
    random_sample: true          # Random sample (vs first K) - prevents bias
  
  # 4-PHASE: parse -> positions -> write -> cleanup
  files_at_once: 1           # PGN files per batch (1=seq, 2-4=batch)
  phase1_threads: all         # PGN parse workers (1=off, all=cpu)
  phase2_threads: all         # Position workers (1=off, all=cpu)


# ------------------------------------------------------------------------------
# MODEL ARCHITECTURE 
# ------------------------------------------------------------------------------
model:
  # Model version identifier
  version: "v5.0"
  # Base architecture
  num_residual_blocks: 8     # Number of residual blocks (6-12 optimal)
  filters: 128               # Convolutional filters (128-256 for RTX 5060 Ti)
  # 🆕 v4.8: Dual-stream policy head
  policy_head_conv_filters: 32  # Spatial stream: 3x3 conv filters (wider = more info preserved)
  policy_head_conv_groups: 4    # Grouped 3x3 conv in policy head (must divide filters and conv_filters)
  policy_head_global_dim: 64    # Global stream: GAP → FC dimension (board-wide context)
  policy_head_hidden_dim: 384   # Intermediate FC dimension (decomposed projection)
  value_head_filters: 64     # Value head filters 
  value_hidden_dim: 384      # Value head hidden dimension 
  dropout: 0.2               # Dropout rate 

  history_positions: 4       # Number of past positions to include (0 = current only, 4 = current + 3 history)
  
  # Advanced features
  use_se2d_blocks: true          # ✅ SE-Net v2 with channel attention
  drop_path_rate: 0.1           # Stochastic Depth
  use_coord_conv: true          # 🗺️ Position-aware conv
  
  # Advanced architectural features
  use_layer_scale: true         # LayerScale for better training stability
  layer_scale_init: 1e-5        # Initial LayerScale value (1e-6 to 1e-4)

  # Multi-Task Learning
  use_multitask_learning: false        # Enable auxiliary tasks
  win_prediction_weight: 0.3          # Weight for win prediction
  material_prediction_weight: 0.15    # Weight for material count
  check_prediction_weight: 0.1        # Weight for check prediction
  
  # 🆕 v4.3: Advanced MTL options
  use_focal_loss_check: true          # Use Focal Loss for check prediction (vs BCE)


# ------------------------------------------------------------------------------
# IMITATION LEARNING (IL)
# ------------------------------------------------------------------------------
imitation_learning:
  # Training hyperparameters
  batch_size: 9216           # Batch size
  learning_rate: 0.0005      # Learning rate 
  value_head_lr_factor: 1.0  # Value head LR multiplier (1.0 = full LR)
  epochs: 30                 # Maximum training epochs
  weight_decay: 0.002        # L2 regularization (10x stronger for value head)
  grad_clip: 1.0             # Gradient clipping threshold
  
  # 🆕 Stochastic Weight Averaging (SWA) for better generalization
  use_swa: true              # Enable SWA (averages weights from multiple epochs)
  swa_start_epoch: 8         # Start SWA after this epoch
  swa_lr: 0.0001             # SWA learning rate (lower than base LR)
  
  # Advanced training features
  label_smoothing: 0.08      # Label smoothing (0.0 = off, 0.1 = standard)
  scheduler_type: 'cosine_decay'  # Options: 'onecycle', 'cosine_decay', 'reduce_on_plateau', 'none'
  warmup_pct: 0.05           # Warmup fraction of epochs (e.g., 0.02-0.05)
  min_lr_ratio: 0.05         # Min LR = base_lr * ratio
  
  # 🆕 v4.3: WDL-specific options
  wdl_label_smoothing: 0.03  # 🔧 v4.8: 0.08->0.03 (0.08 too strong for 3-class WDL)
  # Loss weights
  policy_loss_weight: 1.0    # Policy loss weight
  value_loss_weight: 1.0     # Value loss weight (balanced with policy)
  # 🆕 WDL value weighting by move index (later positions = stronger signal)
  value_move_weighting: true
  value_move_weight_min: 0.1
  value_move_weight_use_game_length: true
  value_move_weight_min_total_moves: 40
  
  # Evaluation & checkpointing
  eval_every: 1              # Evaluate every N epochs
  checkpoint_every: 5        # Save checkpoint every N epochs
  
  # Early stopping
  max_patience: 5           # ℹ️ Stop if no improvement for N epochs
  min_delta: 0.001           # Minimum improvement threshold


# ------------------------------------------------------------------------------
# REINFORCEMENT LEARNING (RL)
# ------------------------------------------------------------------------------
reinforcement_learning:
  # 🚀 Self-play configuration (PARALLEL)
  use_prioritized_replay: true        # Enable prioritized experience replay
  games_per_iteration: 100   # 🆕 More games per iteration (CPU self-play friendly)
  iterations: 1000           # Total RL iterations
  self_play_device: "cpu"    # cpu | cuda | auto (auto uses GPU if available)
  self_play_workers: auto    # 0/auto = cpu_count - 1, or set explicit number
  self_play_torch_threads: 1 # Threads per worker (avoid oversubscription on Windows)

  self_play_save_every_games: 1  # Multiplier of games_per_iteration (0=off, e.g. 0.8 or 2.0)
  
  # 🚀 Batch self-play mode
  use_batch_selfplay: true         # 🚀 NEW: Play multiple games simultaneously (5-10x faster)
  max_batch_games_per_worker: 8   # 🚀 NEW: Max games per worker in batch mode (adjust for VRAM)

  # 🆕 TEMPERATURE SCHEDULE
  use_temperature_schedule: true      # Enable adaptive temperature
  temperature_start: 1.5              # High exploration at start
  temperature_end: 0.5                # Lower exploration at end
  temperature_decay_iterations: 500   # Decay over N iterations

  # 🚀 MCTS parameters (BATCH OPTIMIZED)
  mcts_simulations: 200      # 🆕 Balanced for CPU self-play
  mcts_batch_size: 64        # Batch MCTS evaluations (CPU-friendly)
  mcts_reuse_tree: true      # Reuse tree across moves (faster, better)
  mcts_c_puct: 1.5           # Exploration constant (UCB)
  mcts_temperature: 1.0      # Temperature for move selection
  mcts_temperature_threshold: 15  # Move number to reduce temperature
  mcts_dirichlet_alpha: 0.3  # Dirichlet noise alpha
  mcts_dirichlet_weight: 0.25  # Weight of Dirichlet noise
  
  # Training parameters
  batch_size: 4096           # Training batch size (GPU)
  learning_rate: 0.0002       # Base learning rate
  weight_decay: 0.05         # L2 regularization (AdamW)
  grad_clip: 0.5             # Gradient clipping
  train_epochs_per_iteration: 8  # 🆕 More replays per iteration
  # Replay buffer sizing:
  # Actual size = games_per_iteration * avg_moves_per_game * replay_buffer_multiplier
  avg_moves_per_game: 50        # Estimated average moves per game
  replay_buffer_multiplier: 3   # Buffer size multiplier (iterations worth of data)
  replay_fp16: true            # Store replay tensors in float16 to reduce RAM (~2x)
  
  # 🆕 Learning rate schedule
  use_lr_schedule: true
  lr_warmup_iterations: 20    # Warmup phase
  lr_max: 0.0003              # Peak LR (higher than base)
  lr_min: 0.00005             # Final LR
  
  # 🆕 Target network (DQN-style stability)
  use_target_network: true
  target_update_freq: 5       # Update target every N iterations
  target_polyak: 0.995        # Soft update: θ_target = τ*θ + (1-τ)*θ_target
  
  # 🆕 Entropy regularization (prevent overconfident policy)
  entropy_weight: 0.01        # Policy entropy bonus
  
  # 🆕 Data augmentation (8x more data for free!)
  use_augmentation: true
  augment_horizontal_flip: true   # Mirror board horizontally
  augment_prob: 0.5               # 50% chance per position
  
  # Evaluation
  eval_games: 50             # 🆕 Increased 30→50 for more reliable win rate
  eval_every: 5              # 🆕 Changed 2→5 (less frequent but more data between evals)
  win_rate_threshold: 0.55   # 🆕 More demanding (was 0.53)
  checkpoint_every: 10        # Save checkpoint every N iterations
  
  # Loss weights
  policy_loss_weight: 1.0    # Policy loss weight
  value_loss_weight: 1.0     # Value loss weight


# ------------------------------------------------------------------------------
# HARDWARE & OPTIMIZATION
# ------------------------------------------------------------------------------
hardware:
  device: "cuda"             # Device: cuda, cpu, or mps (Mac)
  
  # Performance optimizations
  use_amp: true              # 🚀 Mixed Precision Training (~2x speedup)
  use_bfloat16: true         # 💾 bfloat16 instead of float16 (better range)
  num_workers: 4             # CPU workers for data loading
  prefetch_factor: 2         # DataLoader prefetch factor
  pin_memory: true           # Faster GPU data transfer


# ------------------------------------------------------------------------------
# LOGGING
# ------------------------------------------------------------------------------
logging:
  log_level: "INFO"          # DEBUG, INFO, WARNING, ERROR
  tensorboard: true          # Enable TensorBoard logging
  print_every: 10            # Print stats every N batches


# ------------------------------------------------------------------------------
# 🆕 DEBUG & PROFILING
# ------------------------------------------------------------------------------
debug:
  enabled: false                    # Master switch for debug mode
  profile_training: true           # Time breakdown per epoch
  log_gpu_memory: false             # GPU memory usage
  profile_every_n_epochs: 1         # Profile frequency (if enabled)
  print_batch0_diagnostics: false   # Print batch-0 target stats + WDL samples


# ------------------------------------------------------------------------------
# REPRODUCIBILITY
# ------------------------------------------------------------------------------
seed: 42                     # Random seed

